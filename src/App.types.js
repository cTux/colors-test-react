/**
 * @typedef AppProps
 * @property {Number} difficulty
 */

/**
 * @typedef AppPoints
 * @property {Number} total
 * @property {Number} correct
 * @property {Number} wrong
 */

/**
 * @typedef AppLevelColor
 * @property {Array<Number>} value
 * @property {Boolean} isCorrect
 */

/**
 * @typedef AppLevel
 * @property {Number} difficulty
 * @property {Array<AppLevelColor>} colors
 */

/**
 * @typedef AppState
 * @property {AppPoints} points
 * @property {AppLevel} level
 */
